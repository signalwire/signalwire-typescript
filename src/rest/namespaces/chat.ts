/**
 * Chat API namespace — token creation.
 *
 * The `Chat` resource class is generated from the chat OpenAPI spec
 * (`chat.resources.generated.ts`); this module re-exports it (plus the
 * `ChatResource` back-compat alias) so existing imports keep working.
 */

export { Chat, Chat as ChatResource } from './chat.resources.generated.js';
