/**
 * SIP Profile namespace — get and update project SIP profile.
 *
 * Generated from the relay-rest OpenAPI spec; re-exported here (with the
 * `SipProfileResource` back-compat alias) so existing imports keep working.
 */

export { SipProfile, SipProfile as SipProfileResource } from './relay-rest.resources.generated.js';
