/**
 * PubSub API namespace — token creation.
 *
 * The `PubSub` resource class is generated from the pubsub OpenAPI spec
 * (`pubsub.resources.generated.ts`); this module re-exports it (plus the
 * `PubSubResource` back-compat alias) so existing imports keep working.
 */

export { PubSub, PubSub as PubSubResource } from './pubsub.resources.generated.js';
